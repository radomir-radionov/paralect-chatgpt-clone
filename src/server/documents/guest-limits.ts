/** Max uploaded documents per anonymous session. */
export const GUEST_ANON_MAX_DOCS = 10;

/** Max total extracted text (chars) across all guest documents for one session. */
export const GUEST_ANON_MAX_TOTAL_TEXT_CHARS = 500_000;
