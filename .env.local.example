# Supabase project (anon + service role for server).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SECRET_KEY=
GUEST_CHAT_QUOTA_SECRET=
OPENAI_API_KEY=
GOOGLE_GENERATIVE_AI_API_KEY=
GROQ_API_KEY=
UNSTRUCTURED_API_KEY=
UNSTRUCTURED_API_URL=
# Supabase Auth (Dashboard → Authentication → URL Configuration):
# - Site URL: http://localhost:3000 in dev; your production HTTPS origin in prod.
# - Redirect URLs: allow http://localhost:3000/** and https://your-production-domain/**
#   so OAuth (e.g. Google) can return to `/` and the app can exchange the code.
